# AchriDZ – سوق المستعمل في الجزائر (Marketplace 69 Wilayas & 200 DZD)

## 1. المبدأ الأساسي للمنصة
> **«لا إعلان منشور بدون دفع رسوم النشر 200 دج والتحقق من العملية عبر 69 ولاية جزائرية.»**

### قاعدة حسابات التجريب (Compte Démo Sandbox):
- **حسابات التجريب لا يمكنها النشر فعليًا للجمهور العام لكنها تمكن من تجريب التطبيق كاملاً لا غير**:
  - يستطيع المستخدم التجريبي تجريب إنشاء إعلان واختيار الولاية من الـ **69 ولاية جزائرية (01 أدرار إلى 69 دبدو)** والمرحلة 10 لدفع 200 دج، وتظهر تجريبيًا في حسابه للمعاينة، لكن لا يتم بث الإعلانات التجريبية في الصفحة الرئيسية أو محرك البحث العام.

- كل إعلان يُنشأ يبدأ في حالة `PAYMENT_REQUIRED`.
- يدفع المعلن مبلغ **200 دج ثابت غير قابل للتعديل** إلى حساب صاحب المنصة عبر حوالة بريدية **CCP** أو تطبيق **BaridiMob**.
- يرفع المعلن رقم العملية (Référence) وصورة إيصال التحويل، وتنتقل حالة الإعلان إلى `PAYMENT_PENDING`.
- يراجع المشرف في لوحة الإدارة (`/admin/payments`) إيصال الدفع ويضغط:
  - **«Valider le paiement»**: تصبح حالة الدفع `VERIFIED` والإعلان `PUBLISHED` ويصل إشعار للمعلن: *«تم تأكيد دفع 200 دج وتم نشر إعلانك بنجاح.»*
  - **«Refuser»**: تصبح حالة الدفع `REJECTED` ويصل إشعار للمعلن: *«تعذر التحقق من عملية الدفع. يرجى مراجعة إثبات الدفع وإعادة الإرسال.»*

---

## 2. حسابات الاختبار السريعة (بيانات تجريبية غير حقيقية)
- **حساب الإدارة (Admin CCP)**:
  - رقم الهاتف: `0550000000` (رمز OTP: `123456` أو زر الدخول السريع بنقرة واحدة)
- **بائع جزائري (أمين قاسي)**:
  - رقم الهاتف: `0661234567`
- **مشتري جزائري (كريم منصوري)**:
  - رقم الهاتف: `0559876543`

---

## 3. إعدادات النشر على Android و iOS و Web
1. **الويب (Web - Production Next.js App Router)**:
   - يعمل على Vercel أو أية حاوية Docker مع قاعدة بيانات PostgreSQL محليًا أو سحابيًا (Supabase/Neon).
2. **تطبيقات الأندرويد و iOS (Capacitor / React Native Wrapper)**:
   - تم تصميم الواجهة بنظام Bottom Navigation المتجاوب وتنسيق الهاتف الذكي المغاربي مع دعم العربية RTL والفرنسية LTR.
   - لتغليف التطبيق كـ APK / IPA:
     ```bash
     npm install @capacitor/core @capacitor/cli
     npx cap init AchriDZ com.achridz.marketplace
     npx cap add android
     npx cap add ios
     npx cap sync
     ```

---

## 4. القابلية للتطوير المستقبلي
- بنية الجداول (`payments`, `platform_settings`, `listings`) مهيأة لدعم:
  - **الدفع الإلكتروني المباشر (CIB / البطاقة الذهبية Edahabia / Satim)**
  - **شركات التوصيل الجزائرية (Yalidine, Maystro Delivery, Procolis)** عبر ربط الـ API بالولاية والبلدية.
  - **الإعلانات المميزة والاشتراكات الشهرية للتجار**.
